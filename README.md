# dotfiles

Personal configuration files.

## Usage

```bash
git clone https://github.com/uncle-monster/dotfiles.git ~/dotfiles
ln -sf ~/dotfiles/tmux.conf ~/.tmux.conf
tmux source ~/.tmux.conf
```

## Contents

- `tmux.conf` - tmux configuration (prefix: Ctrl+a, mouse support, blue/magenta theme)
